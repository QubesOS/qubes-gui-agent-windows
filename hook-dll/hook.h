#include <Windows.h>

LRESULT CALLBACK CallWndProc(
    int code,
    WPARAM wParam,
    LPARAM lParam
    );

LRESULT CALLBACK GetMsgProc(
    int code,
    WPARAM wParam,
    LPARAM lParam
    );

LRESULT CALLBACK CBTProc(
    int code,
    WPARAM wParam,
    LPARAM lParam
    );
